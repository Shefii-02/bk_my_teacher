import 'package:flutter/material.dart';

class MyClassList extends StatelessWidget {
  const MyClassList({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Currently no class found..."));
  }
}
